<% content_for :head do %>
<title>tripbyme.ch</title>

<meta name="viewport" content="initial-scale=1.0, user-scalable=no" />
<%= stylesheet_link_tag "search.css" %>
<%= javascript_include_tag "https://ajax.googleapis.com/ajax/libs/jquery/1.5.1/jquery.min.js" %>
<%= javascript_include_tag "http://maps.google.com/maps/api/js?sensor=false" %>
<%= javascript_include_tag "4sq_search.js" %>
<script type="text/javascript">
  var markers = new Array();
  
  $(initialize);
  
  function initialize() {
    
    var latlng = new google.maps.LatLng(46.529816, 6.630592);
    var myOptions = {
      zoom: 16,
      center: latlng,
      mapTypeId: google.maps.MapTypeId.ROADMAP,
      mapTypeControl: false,
      panControl: false,
      zoomControl: true,
      streetViewControl: false,
    };
    var map = new google.maps.Map(document.getElementById("map_canvas"), myOptions);
    
    // debug: do it only once, for testing.
    // fsq_search(map.getCenter(), map);
    
    google.maps.event.addListener(map, 'bounds_changed', function() {
      //fsq_search(map.getCenter(), map);
    });
  }
</script>
<% end %>

<div id="timeline">
  <div id="timeline_content">
    asd
  </div>
</div>

<div id="location">
<div id="results">
  <div id="result_items">
  <div id="search_container">
    <input id="searchbox" type="text" value="Search..." placeholder="Search..."></input>
  </div>
  <ul></ul>
  </div>
</div>
<div class="map" id="map_container"><div id="map_canvas"></div></div>
</div>
